# 개발일지 - 2026-03-11 (대시보드 불필요 정보 제거 및 레이아웃 재배치)

- **작성시각**: 2026-03-11 21:05
- **해결하고자 한 문제**: 사용자의 의도("업로딩한 이미지 2장 부분 삭제후 재배치")에 맞게, 홈 대시보드에서 산만함을 주던 "글로벌 시장" 정보와 "종목 목록" 사이드바를 완전히 걷어내고 필수 요소들만 남겨서 집중도를 높이는 레이아웃 개편 작업.

## 해결된 것
- 사용자 오해로 인해 잘못 변경되었던 `MarketOverview.tsx`와 `StockList.tsx`의 컴포넌트 내부 소스 코드를 모두 기존 상태로 롤백(복구) 처리.
- `Index.tsx`에서 `<StockList />`, `<MarketOverview />` 컴포넌트 렌더링 영역 및 연관 사이드바 레이아웃 삭제.
- 삭제된 공간의 여백을 활용해 `Index.tsx`의 중앙 차트 영역과 진단 배너의 크기/폰트를 자연스럽게 스케일업(`text-lg`, 패딩 등 확대).
- 홈 화면의 AI 진단 서비스 진입 배너 복구 및 시인성 강화.

## 비고
- 한 화면에 많은 뷰(시장 지수, 차트, 리스트 등)를 구겨넣기보다 꼭 필요한 정보들만 배치하여 진단 서비스 본연의 목적감을 살리는 긍정적인 UX 개선이 이뤄짐.
