# 개발일지 - 2026-03-11 (라우팅 변경)

- **작성시각**: 2026-03-11 20:40
- **해결하고자 한 문제**: 프로젝트 랜딩 페이지를 홈(`/home`)에서 진단(`/`) 페이지로 변경하고, 진단(검색) 후 자연스럽게 홈으로 넘어가는 플로우 구성.

## 해결된 것
- `App.tsx`의 라우트 경로 수정 완료 (`/` -> `Diagnose`, `/home` -> `Index`).
- `AppShell.tsx`에서 모바일 하단 탭과 데스크톱 메뉴의 "홈" 경로 및 활성화 조건 수정 완료.
- `Diagnose.tsx`에서 종목 검색(진단) 후 로딩 스피너 종료 시 결과 모달 대신, 홈(`/home`) 페이지로 자동 이동(`useNavigate`)되도록 라우팅 로직 처리 완료.
- `Diagnose.tsx` 내 "홈으로" 뒤로가기 버튼 타겟 수정.
- `Index.tsx` 내 "이 종목, 지금 사도 될까요?" 배너 링크 타겟 수정.

## 해결되지 않은 것
- 없음 (모든 연결 오류 수정 완료).

## 비고
- 기존 진단 결과 모달은 사라지지만 사용성 측면에서 홈 화면의 대시보드(차트 및 결과 패널)로 흐름을 집중시킴.
