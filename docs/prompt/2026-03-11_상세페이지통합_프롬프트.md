# 프롬프트 로그 - 2026-03-11 (홈 화면 및 상세 페이지 통합)

- **작성시각**: 2026-03-11 21:28
- **요구사항**: 개별 진입하던 `/stock/:symbol` 상세 페이지를 완전히 폐지하고 홈 화면(`/home` 또는 `Index.tsx`) 한 곳에서 모든 상세 정보가 종목별로 전환되게끔 시스템 효율화.

## 작업 내용 및 흐름
1. `Index.tsx` 파일 내에 `StockDetail.tsx`의 탭(차트, 재무제표, 뉴스, 목표가) 컴포넌트 이식 준비.
2. `Index.tsx`는 기존에 `selectedSymbol` state 프레임워크를 기반으로 싱글 페이지 뷰를 띄워주고 있었으므로, 그 위치에 맞추어 `FinancialSummary`, `NewsFeed`, `AnalystTargets` 등을 import 한 뒤 렌더링.
3. 시원시원한 카드 기반의 여백(마진/패딩)과 `AppShell`의 Ticker 등을 위화감 없이 연결하도록 마크업 구조 최적화.
4. 더 이상 쓰이지 않는 `StockDetail.tsx` 자체를 삭제 처리.
5. 관련하여 외부에서 상세 페이지를 띄우던 링크들(`Watchlist`, `PortfolioTable`, `StockList` 등의 `<Link>` 및 `navigate()`)의 대상을 모두 `/home`으로 변경하면서 `state: { symbol }` 컨텍스트를 주입하도록 라우팅 일괄 수정.
6. 개발일지 및 `walkthrough.md`, 태스크 보드 현행화 달성.
