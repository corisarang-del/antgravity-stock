# 프롬프트 로그 - 2026-03-11 (라우팅 변경)

- **작성시각**: 2026-03-11 20:42
- **태스크**: `/diagnose`를 기본 랜딩 페이지(`/`)로 만들고, 검색(진단) 후 `/home`으로 라우팅.

## 사용된 주요 흐름 및 전략

1. **상태 파악**:
   - `App.tsx`, `AppShell.tsx`, `Diagnose.tsx`, `Index.tsx`의 각 라우팅 상호작용 지점 분석.

2. **단순화 및 플로우 개편**:
   - `Diagnose.tsx`에서 결과를 보여주는 UI(AnimatePresence 내부) 부분을 걷어내기보다, 자연스럽게 이동하는 UX를 위해 결과 상태값 `setDiagnosed(stock)` 대신 `navigate("/home")`을 `setTimeout` 내부에 삽입하여 단순하고 확실하게 처리.

3. **연관 컴포넌트 처리**:
   - URL 체계 변경(`/home` 도입)에 따라 `AppShell.tsx`의 탭 리스트(`TABS`)와 네비게이션 링크들을 일괄적으로 찾아서 수정(replace_file_content 활용).
   - "홈으로 가기"나 "진단하러 가기" 버튼들이 서로 엉키지 않도록 교차 확인 후 수정.
