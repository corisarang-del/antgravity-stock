# 프롬프트 로그 - 2026-03-11 (Stockray 벤치마킹 반영)

- **작성시각**: 2026-03-11 20:50
- **태스크**: 종목 검색 후 점수/등급 표출 -> 상세 보기 클릭 시 대시보드 `/home`으로 파라미터를 넘겨 렌더링하는 UX 최적화.

## 사용된 명령 및 흐름
1. `browser_subagent`를 통해 `https://stockray.app`에 접속하여 검색 플로우 분석.
   - 분석 결과: "검색 -> AI 스피너 노출 -> 분석 점수 텍스트 표출 (스킵 안 됨) -> 회원 등급별 확인 가능 플로우"
2. 기존에 임시로 스킵 적용된 `Diagnose.tsx`의 `navigate("/home")` 원상 복구. (`setDiagnosed` 로직 살림)
3. "홈" 대시보드의 유연성을 확보하기 위해 `useLocation` `state` 활용하여 종목 간 데이터 전달(`Index.tsx`에서 초기 마운트 시 `symbol`을 취득해 `selectedSymbol` 상태 업데이트).
